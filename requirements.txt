pandas
numpy
gradio
matplotlib
seaborn
scikit-learn
scipy
tqdm
pytest

# Text generation metrics (lexical)
sacrebleu>=2.3.0
rouge-score>=0.1.2
nltk>=3.8.1
bert-score>=0.3.13

# LLM-judge provider router + CRIMSON
# (GREEN and RadFact are deliberately omitted: GREEN needs GPU with pinned torch 2.2.2,
# and RadFact pins pydantic 1.x which conflicts with Gradio >=5.)
litellm>=1.40.0
crimson-score>=0.2.0
