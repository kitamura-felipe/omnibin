pandas
numpy
gradio
matplotlib
seaborn
scikit-learn
scipy
tqdm
pytest

# Text generation metrics (lexical)
sacrebleu>=2.3.0
rouge-score>=0.1.2
nltk>=3.8.1
bert-score>=0.3.13

# LLM-judge provider router
# (GREEN and RadFact run in API mode on the Space — they don't need the
# upstream packages here. CRIMSON is deliberately omitted: crimson-score
# pins pandas>=3.0.1 which conflicts with Gradio's pandas<3.0. Install
# `omnibin[crimson]` locally to use CRIMSON's official package.)
litellm>=1.40.0
